name: jinocaflix
description: Meu app de animes e mangás
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  html: ^0.15.4
  cached_network_image: ^3.3.1
  better_player: ^0.0.83
  photo_view: ^0.14.0

flutter:
  uses-material-design: true
